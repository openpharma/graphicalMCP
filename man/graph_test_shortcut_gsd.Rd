% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_test_shortcut_gsd.R
\name{graph_test_shortcut_gsd}
\alias{graph_test_shortcut_gsd}
\title{Perform shortcut graphical multiple comparison procedures with group
sequential designs}
\usage{
graph_test_shortcut_gsd(
  graph,
  p,
  alpha = 0.025,
  info_frac,
  spending_fn,
  look_back = FALSE,
  verbose = FALSE,
  test_values = FALSE
)
}
\arguments{
\item{graph}{An initial graph as returned by \code{\link[=graph_create]{graph_create()}}.}

\item{p}{A numeric matrix of p-values with \eqn{m} rows (hypotheses) and
\eqn{K} columns (analyses), where \eqn{K} is the maximum number of
analyses across all hypotheses. For hypotheses not tested at every
analysis, use \code{NA} for the columns without data. Each hypothesis
must have at least one non-\code{NA} value.}

\item{alpha}{A numeric scalar of the overall significance level, which should
be between 0 & 1. The default is 0.025 for one-sided hypothesis testing
problems; another common choice is 0.05 for two-sided hypothesis testing
problems.}

\item{info_frac}{Information fractions at each analysis. Can be:
\itemize{
\item A numeric vector of length \eqn{K} — same fractions for all hypotheses.
Only allowed when \code{p} contains no \code{NA} values (i.e., all hypotheses
have the same number of analyses).
\item A numeric matrix with \eqn{m} rows (hypotheses) and \eqn{K} columns
(analyses) — different fractions per hypothesis. When \code{p} contains
\code{NA} padding, \code{info_frac} must be a matrix with \code{NA} in the same
positions as \code{p}.
}

Non-\code{NA} values must be in (0, 1] and monotonically non-decreasing per
hypothesis. The last non-\code{NA} value does not need to be 1, allowing
the procedure to be applied up to an interim analysis.}

\item{spending_fn}{Spending function(s) for computing group sequential
boundaries. Can be:
\itemize{
\item A single function — applied to all hypotheses.
\item A list of \eqn{m} functions — one per hypothesis.
}

Each function must accept two arguments: \code{alpha} (significance level)
and \code{info_frac} (information fraction), and return the cumulative alpha
spent. Built-in options include \code{\link[=spending_of]{spending_of()}}, \code{\link[=spending_pocock]{spending_pocock()}},
\code{\link[=spending_hsd]{spending_hsd()}}, and \code{\link[=spending_linear]{spending_linear()}}.}

\item{look_back}{A logical scalar controlling the testing strategy.
If \code{FALSE} (default), rejection decisions at each analysis are based on
repeated p-values at that analysis only. If \code{TRUE}, rejection decisions
are based on sequential p-values which consider all analyses up to the
current one. The \code{look_back = TRUE} option can lead to additional
rejections because a hypothesis may have crossed its boundary at an
earlier analysis but only becomes testable (via graph update) at a later
analysis. The default is \code{FALSE}.}

\item{verbose}{A logical scalar specifying whether the details of
intermediate update graphs should be included in results. When
\code{verbose = TRUE}, intermediate update graphs are provided after deleting
each hypothesis, which has been rejected. The default is \code{verbose = FALSE}.}

\item{test_values}{A logical scalar specifying whether to include the
per-analysis rejection details in results. When \code{test_values = TRUE}, the
rejection sequence, analysis at which each rejection occurred, and the
nominal p-value boundaries are reported. The default is
\code{test_values = FALSE}.}
}
\value{
An S3 object of class \code{gsd_graph_report} with a list of elements:
\itemize{
\item \code{inputs} - Input parameters, including the initial graph, p-values,
alpha, information fractions, and spending functions.
\item \code{outputs} - Output parameters:
\itemize{
\item \code{repeated_p} - An m x K matrix of repeated p-values at each analysis,
\item \code{sequential_p} - An m x K matrix of sequential p-values (cumulative
minimum of repeated p-values),
\item \code{adjusted_p} - Adjusted p-values from the shortcut procedure
(adjusted repeated p-values when \code{look_back = FALSE}, adjusted
sequential p-values when \code{look_back = TRUE}),
\item \code{rejected} - Logical vector of rejection decisions,
\item \code{decision_at} - Integer vector indicating the analysis at which each
hypothesis's decision was made. For rejected hypotheses, this is
the analysis where the rejection was operationally processed. For
non-rejected hypotheses, this is the last analysis where the
hypothesis was tested,
\item \code{first_rejected_at} - Integer vector indicating the earliest analysis
at which each hypothesis's boundary was crossed. For non-rejected
hypotheses, this is \code{NA}. When \code{look_back = TRUE}, this may be
earlier than \code{decision_at} if a hypothesis crossed its boundary at
a prior analysis but only became testable at a later analysis,
\item \code{rejection_sequence} - Character vector giving the order in which
hypotheses were rejected across all analyses,
\item \code{graph} - Updated graph after removing all rejected hypotheses.
}
\item \code{test_values} - Per-analysis details (if \code{test_values = TRUE}). A list
of length \eqn{K} (one entry per analysis). Each entry is a data frame
containing the hypothesis name, current weight, observed p-value,
nominal boundary, and rejection decision at that analysis. Entries are
\code{NULL} for analyses where no hypotheses are active.
}
}
\description{
Extends \code{\link[=graph_test_shortcut]{graph_test_shortcut()}} to group sequential designs where hypotheses
can be tested at multiple analyses (interim and final). At each analysis,
the significance level available for each hypothesis is determined by a
spending function evaluated at the information fraction. The group
sequential boundaries (critical values) are computed from the spending using
the joint distribution of test statistics across analyses.

The procedure supports two modes
controlled by the \code{look_back} parameter:
\itemize{
\item \strong{\code{look_back = FALSE}} (default): At each analysis, rejection decisions
are based on \strong{repeated p-values} at the current analysis only. A
repeated p-value at analysis \eqn{k} is the minimum significance level at
which the group sequential boundary at analysis \eqn{k} would be crossed.
The graphical shortcut procedure (\code{\link[=graph_test_shortcut]{graph_test_shortcut()}}) is applied at
each analysis using repeated p-values, and the graph is updated before
proceeding to the next analysis.
\item \strong{\code{look_back = TRUE}}: Rejection decisions are based on \strong{sequential
p-values}, which consider all analyses up to the current one. A
sequential p-value is the minimum of repeated p-values across all
analyses conducted so far. Like the default mode, the procedure
processes analyses sequentially, applying \code{\link[=graph_test_shortcut]{graph_test_shortcut()}} at
each analysis using sequential p-values and updating the graph before
proceeding. When a hypothesis becomes testable at a later analysis
(via graph update), its \code{decision_at} is set to the earliest analysis
where its sequential p-value crossed the boundary.
}
}
\examples{
# A graphical procedure with two hypotheses tested at two analyses
hypotheses <- c(0.5, 0.5)
transitions <- rbind(c(0, 1), c(1, 0))
g <- graph_create(hypotheses, transitions)

# P-values at interim (50\% info) and final (100\% info) analyses
p <- rbind(
  H1 = c(0.024, 0.01),
  H2 = c(0.015, 0.005)
)

graph_test_shortcut_gsd(
  graph = g,
  p = p,
  alpha = 0.025,
  info_frac = c(0.5, 1),
  spending_fn = spending_of
)

# With look_back = TRUE (sequential p-values)
graph_test_shortcut_gsd(
  graph = g,
  p = p,
  alpha = 0.025,
  info_frac = c(0.5, 1),
  spending_fn = spending_of,
  look_back = TRUE
)

# Different spending functions per hypothesis
graph_test_shortcut_gsd(
  graph = g,
  p = p,
  alpha = 0.025,
  info_frac = c(0.5, 1),
  spending_fn = list(spending_of, spending_pocock)
)

# User-defined spending functions can also be used, e.g., wrapping
# gsDesign::sfHSD(). See vignette("group-sequential-testing") for details.

# Different information fractions per hypothesis
graph_test_shortcut_gsd(
  graph = g,
  p = p,
  alpha = 0.025,
  info_frac = rbind(c(0.5, 1), c(0.6, 1)),
  spending_fn = spending_of
)

# Different numbers of analyses per hypothesis (NA padding)
# H1 at analyses 1-2, H2 at 1-3, H3 at 2-3, H4 at 1 and 3
g4 <- graph_create(
  rep(0.25, 4),
  rbind(
    c(0, 1/3, 1/3, 1/3),
    c(1/3, 0, 1/3, 1/3),
    c(1/3, 1/3, 0, 1/3),
    c(1/3, 1/3, 1/3, 0)
  )
)
p4 <- rbind(
  H1 = c(0.024, 0.01, NA),
  H2 = c(0.015, 0.005, 0.001),
  H3 = c(NA, 0.012, 0.004),
  H4 = c(0.05, NA, 0.015)
)
# info_frac must be a matrix with NA matching p
info_frac4 <- rbind(
  H1 = c(0.5, 1, NA),
  H2 = c(1/3, 2/3, 1),
  H3 = c(NA, 0.5, 1),
  H4 = c(0.4, NA, 1)
)
graph_test_shortcut_gsd(
  graph = g4,
  p = p4,
  alpha = 0.025,
  info_frac = info_frac4,
  spending_fn = spending_of
)
}
\references{
Maurer, W., and Bretz, F. (2013). Multiple testing in group sequential
trials using graphical approaches. \emph{Statistics in Biopharmaceutical
Research}, 5(4), 311-320.

Zhao, Y., Liu, Q., Sun, L. Z., and Anderson, K. M. (2025). Adjusted
inference for multiple testing procedure in group-sequential designs.
\emph{Biometrical Journal}, 67(1), e70020.
\doi{10.1002/bimj.70020}
}
\seealso{
\code{\link[=graph_test_shortcut]{graph_test_shortcut()}} for the fixed-sample (non-sequential) shortcut
procedure, \code{\link[=sequential_p]{sequential_p()}} for computing sequential p-values,
\code{\link[=repeated_p]{repeated_p()}} for computing repeated p-values,
\code{\link[=spending_of]{spending_of()}}, \code{\link[=spending_pocock]{spending_pocock()}}, \code{\link[=spending_hsd]{spending_hsd()}},
\code{\link[=spending_linear]{spending_linear()}} for spending functions.
}
