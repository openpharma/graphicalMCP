% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_test_shortcut_gsd.R
\name{gsd_look_back}
\alias{gsd_look_back}
\title{Apply GSD procedure with look_back = TRUE (sequential p-values)}
\usage{
gsd_look_back(
  graph,
  p,
  alpha,
  info_frac,
  spending_fn,
  num_analyses,
  num_hyps,
  hyp_names,
  analysis_names,
  test_values,
  verbose
)
}
\description{
Processes analyses sequentially, like \code{\link[=gsd_no_look_back]{gsd_no_look_back()}}, but uses
sequential p-values instead of repeated p-values at each analysis.
At each analysis k, the sequential p-values (cumulative minimum of
repeated p-values up to k) are passed to \code{graph_test_shortcut()} for
rejection decisions. The graph is updated after each analysis before
proceeding to the next. This mirrors the operational workflow where
analyses are conducted one at a time.
}
\keyword{internal}
