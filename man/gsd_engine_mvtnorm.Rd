% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsd_engine.R
\name{gsd_engine_mvtnorm}
\alias{gsd_engine_mvtnorm}
\title{Group sequential engines for the graphical procedure}
\usage{
gsd_engine_mvtnorm(spending_fn)
}
\arguments{
\item{spending_fn}{A list of spending functions, one per hypothesis. Each
must accept two arguments, \code{alpha} and \code{info_frac}, and return the
cumulative alpha spent.}
}
\value{
A list with the functions \code{repeated_p} and \code{nominal_bounds}.
}
\description{
The graphical procedure in \code{\link[=graph_test_shortcut_gsd]{graph_test_shortcut_gsd()}} relies on exactly two
group sequential computations: repeated p-values and nominal p-value
boundaries. An \emph{engine} bundles these two computations so that the
graphical logic in \code{gsd_test()} and its helpers is independent of how they
are carried out.

An engine is a list of two functions. Each takes the hypothesis index \code{j}
and closes over that hypothesis's spending specification:
\itemize{
\item \code{repeated_p(p, info_frac, j)} - the repeated p-value for hypothesis \code{j}
at the last of the supplied analyses, given the p-values and information
fractions at all analyses up to and including it.
\item \code{nominal_bounds(alpha, info_frac, j)} - the nominal p-value boundaries
for hypothesis \code{j} at each supplied analysis when the significance level
\code{alpha} is allocated to it.
}

\code{gsd_engine_mvtnorm()} builds the engine backed by \code{\link[=repeated_p]{repeated_p()}} and
\code{\link[=gs_boundaries]{gs_boundaries()}}, which evaluate the joint distribution of the test
statistics with mvtnorm.
}
\keyword{internal}
