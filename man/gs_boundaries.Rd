% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gs_boundaries.R
\name{gs_boundaries}
\alias{gs_boundaries}
\title{Compute group sequential boundaries from an alpha spending function}
\usage{
gs_boundaries(alpha, info_frac, spending_fn, maxpts = 25000, abseps = 1e-06)
}
\arguments{
\item{alpha}{A numeric scalar of the significance level to be spent across
analyses.}

\item{info_frac}{A numeric vector of information fractions at each analysis.
Must be monotonically non-decreasing with values in (0, 1].}

\item{spending_fn}{A spending function that takes two arguments: \code{alpha}
(significance level) and \code{info_frac} (information fraction), and returns
the cumulative alpha spent. See \code{\link[=spending_of]{spending_of()}}.}

\item{maxpts}{An integer scalar for the maximum number of function values
for \code{mvtnorm::GenzBretz}. The default is 25000.}

\item{abseps}{A numeric scalar for the absolute error tolerance for
\code{mvtnorm::GenzBretz}. The default is 1e-6.}
}
\value{
A list with elements:
\itemize{
\item \code{bounds_z} - A numeric vector of Z-scale boundaries at each analysis.
\item \code{bounds_nominal} - A numeric vector of nominal p-value boundaries at
each analysis, i.e., \eqn{c_k = 1 - \Phi(b_k)}.
}
}
\description{
Given a significance level, information fractions, and a spending function,
compute the group sequential boundaries at each analysis. The boundaries are
computed on the Z-scale using the recursive relationship between cumulative
spending and the joint distribution of test statistics. The null hypothesis
is rejected at analysis \eqn{k} if the test statistic \eqn{Z_k \ge b_k}.

At analysis \eqn{k}, the Z-scale boundary \eqn{b_k} satisfies
\deqn{P(Z_1 < b_1, \ldots, Z_k < b_k) = 1 - f(\alpha, t_k),}
where \eqn{f(\alpha, t_k)} is the cumulative spending at information
fraction \eqn{t_k}, and \eqn{(Z_1, \ldots, Z_k)} follows the canonical
joint distribution with mean zero and correlations given by \code{\link[=gs_corr]{gs_corr()}}.
}
\seealso{
\code{\link[=spending_of]{spending_of()}}, \code{\link[=spending_pocock]{spending_pocock()}}, \code{\link[=spending_hsd]{spending_hsd()}}, \code{\link[=spending_linear]{spending_linear()}}
for spending functions, \code{\link[=sequential_p]{sequential_p()}} for sequential p-values,
\code{\link[=graph_test_shortcut_gsd]{graph_test_shortcut_gsd()}} for graphical MCPs with group sequential
designs, \code{\link[=gs_corr]{gs_corr()}} for the correlation matrix.
}
