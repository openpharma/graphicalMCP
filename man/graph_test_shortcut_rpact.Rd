% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_test_shortcut_rpact.R
\name{graph_test_shortcut_rpact}
\alias{graph_test_shortcut_rpact}
\title{Perform shortcut graphical multiple comparison procedures with group
sequential designs using rpact}
\usage{
graph_test_shortcut_rpact(
  graph,
  p,
  alpha = 0.025,
  info_frac,
  typeOfDesign = "asOF",
  gammaA = NULL,
  deltaWT = NULL,
  userAlphaSpending = NULL,
  spending_time = NULL,
  look_back = FALSE,
  verbose = FALSE,
  test_values = FALSE
)
}
\arguments{
\item{graph}{An initial graph as returned by \code{\link[=graph_create]{graph_create()}}.}

\item{p}{A numeric matrix of p-values with \eqn{m} rows (hypotheses) and
\eqn{K} columns (analyses), where \eqn{K} is the maximum number of
analyses across all hypotheses. For hypotheses not tested at every
analysis, use \code{NA} for the columns without data. Each hypothesis
must have at least one non-\code{NA} value.}

\item{alpha}{A numeric scalar of the overall significance level, which should
be between 0 & 1. The default is 0.025 for one-sided hypothesis testing
problems; another common choice is 0.05 for two-sided hypothesis testing
problems.}

\item{info_frac}{Information fractions at each analysis. Can be:
\itemize{
\item A numeric vector of length \eqn{K} — same fractions for all hypotheses.
Only allowed when \code{p} contains no \code{NA} values (i.e., all hypotheses
have the same number of analyses).
\item A numeric matrix with \eqn{m} rows (hypotheses) and \eqn{K} columns
(analyses) — different fractions per hypothesis. When \code{p} contains
\code{NA} padding, \code{info_frac} must be a matrix with \code{NA} in the same
positions as \code{p}.
}

Non-\code{NA} values must be positive and monotonically non-decreasing per
hypothesis. Values greater than 1 are allowed (e.g., when more
information is collected than planned). The spending functions cap
the cumulative spending at \code{alpha} for information fractions at or
above 1. The last non-\code{NA} value does not need to be 1, allowing
the procedure to be applied up to an interim analysis.}

\item{typeOfDesign}{Type of design in rpact's convention, as accepted by
\code{\link[rpact:getDesignGroupSequential]{rpact::getDesignGroupSequential()}}: an alpha spending design \code{"asOF"}
(Lan-DeMets O'Brien-Fleming, the default), \code{"asP"} (Lan-DeMets Pocock),
\code{"asHSD"} (Hwang-Shih-DeCani), \code{"asKD"} (Kim-DeMets power family), or
\code{"asUser"} (user-defined cumulative spending), or one of the classical
boundary families \code{"OF"}, \code{"P"}, \code{"WT"}, \code{"PT"}, \code{"HP"}, \code{"WToptimum"},
\code{"noEarlyEfficacy"}. A single string applies to all hypotheses; a list
of \eqn{m} strings gives one per hypothesis.}

\item{gammaA}{Parameter of the \code{"asHSD"} and \code{"asKD"} designs, passed to
rpact as \code{gammaA}. A single value applies to all hypotheses; a list of
\eqn{m} values gives one per hypothesis. Required for these design types.}

\item{deltaWT}{Parameter \eqn{\Delta} of the \code{"WT"} design, passed to
rpact as \code{deltaWT}. A single value applies to all hypotheses; a list of
\eqn{m} values gives one per hypothesis. Required for \code{"WT"}.}

\item{userAlphaSpending}{Cumulative alpha spent at each analysis for the
\code{"asUser"} design, passed to rpact as \code{userAlphaSpending}: a
non-decreasing vector with one value per analysis of the hypothesis,
between 0 and \code{alpha}. A single vector applies to all hypotheses; a list
of \eqn{m} vectors gives one per hypothesis. When the graph allocates a
level other than \code{alpha} to a hypothesis, the values are scaled
proportionally.}

\item{spending_time}{Optional spending time. Can be \code{NULL} (the default, in
which case the spending time is \code{pmin(info_frac, 1)}), a numeric vector
of length \eqn{K} applied to all hypotheses (only allowed when \code{p}
contains no \code{NA}), or an \eqn{m \times K} matrix with \code{NA} in the same
positions as \code{info_frac}. It controls how \code{alpha} is spent across
analyses, separately from \code{info_frac}, which controls the correlation
between analyses, and plays the role of \code{usTime} in
\code{\link[=graph_test_shortcut_gsDesign]{graph_test_shortcut_gsDesign()}} and of \code{\link[=spending_with_time]{spending_with_time()}} in
\code{\link[=graph_test_shortcut_gsd]{graph_test_shortcut_gsd()}}. It does not apply to the classical boundary
families or to \code{"asUser"} and is ignored, with a warning, for hypotheses
that use them.}

\item{look_back}{A logical scalar or vector controlling the testing strategy.
Can be:
\itemize{
\item A single logical — applied to all hypotheses.
\item A logical vector of length \eqn{m} — one per hypothesis, allowing
different strategies for different hypotheses.
}

For hypotheses with \code{look_back = FALSE} (the default), rejection decisions
at each analysis are based on repeated p-values at that analysis only.
For hypotheses with \code{look_back = TRUE}, rejection decisions are based on
sequential p-values which consider all analyses up to the current one.
The \code{look_back = TRUE} option can lead to additional rejections because
a hypothesis may have crossed its boundary at an earlier analysis but
only becomes testable (via graph update) at a later analysis.}

\item{verbose}{A logical scalar specifying whether to include the boundary
table in results. When \code{verbose = TRUE}, a table of nominal p-value
boundaries is computed for each hypothesis at all possible weights from
the graph's closure (via \code{\link[=graph_generate_weights]{graph_generate_weights()}}). This enables manual
verification of rejection decisions. The default is \code{FALSE}.}

\item{test_values}{A logical scalar specifying whether to include the
per-analysis rejection details in results. When \code{test_values = TRUE}, the
rejection sequence, analysis at which each rejection occurred, and the
nominal p-value boundaries are reported. The default is
\code{test_values = FALSE}.}
}
\value{
An S3 object of class \code{gsd_graph_report} with the same elements as
\code{\link[=graph_test_shortcut_gsd]{graph_test_shortcut_gsd()}}. \code{inputs} records \code{typeOfDesign}, \code{gammaA},
\code{deltaWT}, \code{userAlphaSpending}, and \code{spending_time} in place of
\code{spending_fn}, and \code{inputs$engine} is \code{"rpact"}.
}
\description{
Extends \code{\link[=graph_test_shortcut]{graph_test_shortcut()}} to group sequential designs in the same way
as \code{\link[=graph_test_shortcut_gsd]{graph_test_shortcut_gsd()}} and \code{\link[=graph_test_shortcut_gsDesign]{graph_test_shortcut_gsDesign()}}, but
delegates all group sequential calculations (boundaries and repeated
p-values) to the rpact package. Designs are specified in rpact's own terms:
a design type \code{typeOfDesign} with its parameters \code{gammaA}, \code{deltaWT}, or
\code{userAlphaSpending}, and an optional spending time. The graphical
procedure, the two testing modes selected by \code{look_back}, and the structure
of the results are identical to \code{\link[=graph_test_shortcut_gsd]{graph_test_shortcut_gsd()}}; see that
function for a description of the procedure and of \code{look_back}.

rpact is an optional dependency: the function stops with an informative
error if it is not installed.
}
\details{
This function performs the same graphical group sequential procedure as
\code{\link[=graph_test_shortcut_gsd]{graph_test_shortcut_gsd()}} (Maurer and Bretz, 2013), but delegates all
group sequential calculations to rpact, used only through its public
function \code{\link[rpact:getDesignGroupSequential]{rpact::getDesignGroupSequential()}}.
\itemize{
\item \strong{Analysis structure.} For hypothesis \eqn{j} with information
fractions \eqn{t_j} at its \eqn{K_j} analyses, a one-sided design
\code{getDesignGroupSequential(kMax = K_j, informationRates = t_j, alpha, sided = 1, typeOfDesign, ...)} encodes the schedule, and is re-evaluated
at whatever level the graph currently allocates to the hypothesis (its
weight times \code{alpha}). A schedule may end before full information, as
when only the analyses conducted so far are supplied.
\item \strong{Nominal boundaries.} For \code{test_values} and \code{boundary_table}, the
boundary at analysis \eqn{k} for an allocated level is the design's
\code{stageLevels[k]} (rpact's nominal one-sided significance level).
\item \strong{Repeated p-values.} The repeated p-value at analysis \eqn{k} is the
smallest level at which the boundary at analysis \eqn{k} is crossed. It
is found by solving \eqn{c_k(\alpha) = Z_k} on the design's
\code{criticalValues} over the level. This is the algorithm that rpact's
\code{\link[rpact:getRepeatedPValues]{rpact::getRepeatedPValues()}} uses internally, applied without its
ceiling of 0.5 and to every design type. rpact cannot compute designs at
levels above roughly 0.65; a boundary that is not crossed at the highest
level rpact can compute is reported as a repeated p-value of 1.
Sequential p-values (\code{look_back = TRUE}) are the running minimum of the
repeated p-values.
\item \strong{Spending time and over-running information.} rpact evaluates the
spending function at the design's information rates and does not accept
rates above 1. When \code{spending_time} differs from \code{info_frac}, or when
\code{info_frac} exceeds 1 at the final analysis, the design is built in two
steps with rpact's \code{"asUser"} type: the cumulative spending is taken from
a design at the spending time and passed as \code{userAlphaSpending} to a
design at the information fractions, divided by their maximum when they
exceed 1 (the correlation between analyses depends only on their
ratios). The classical boundary families are defined by their boundary
shape over the whole planned schedule, and rpact requires their final
information fraction to be exactly 1: unlike the alpha spending designs,
they cannot be given only the analyses conducted so far.
\item \strong{Single-analysis hypotheses.} A hypothesis with one analysis below
full information uses the first stage of a two-stage design ending at 1,
so that its boundary is the cumulative spending at that information
fraction; at full information it is a fixed-sample test.
}

rpact warns when a level lies outside its validated range of
\eqn{[10^{-6}, 0.5)}; the graphical procedure evaluates such levels
routinely (for small hypothesis weights, and when searching for repeated
p-values), so these warnings are suppressed. rpact's integration is
deterministic, so results are reproducible without setting a seed. The
built-in spending functions correspond to \code{typeOfDesign = "asOF"}
(\code{\link[=spending_of]{spending_of()}}), \code{"asP"} (\code{\link[=spending_pocock]{spending_pocock()}}), \code{"asHSD"} with
\code{gammaA = gamma} (\code{\link[=spending_hsd]{spending_hsd()}}), \code{"asKD"} with \code{gammaA = 1}
(\code{\link[=spending_linear]{spending_linear()}}), and \code{"WT"} with \code{deltaWT = delta}
(\code{\link[=spending_wt]{spending_wt()}}).
}
\examples{
if (requireNamespace("rpact", quietly = TRUE)) {
  # A graphical procedure with two hypotheses tested at two analyses
  hypotheses <- c(0.5, 0.5)
  transitions <- rbind(c(0, 1), c(1, 0))
  g <- graph_create(hypotheses, transitions)

  # P-values at interim (50\% info) and final (100\% info) analyses
  p <- rbind(
    H1 = c(0.024, 0.01),
    H2 = c(0.015, 0.005)
  )

  # Lan-DeMets O'Brien-Fleming alpha spending (the default)
  graph_test_shortcut_rpact(g, p, alpha = 0.025, info_frac = c(0.5, 1))

  # Hwang-Shih-DeCani spending with gamma = -2, using sequential p-values
  graph_test_shortcut_rpact(
    g, p,
    alpha = 0.025, info_frac = c(0.5, 1),
    typeOfDesign = "asHSD", gammaA = -2, look_back = TRUE
  )

  # Different designs per hypothesis
  graph_test_shortcut_rpact(
    g, p,
    alpha = 0.025, info_frac = c(0.5, 1),
    typeOfDesign = list("asOF", "asP")
  )

  # A spending time that differs from the information fraction
  graph_test_shortcut_rpact(
    g, p,
    alpha = 0.025, info_frac = c(0.5, 1), spending_time = c(0.4, 1)
  )

  # Wang-Tsiatis boundaries with Delta = 0.25
  graph_test_shortcut_rpact(
    g, p,
    alpha = 0.025, info_frac = c(0.5, 1), typeOfDesign = "WT", deltaWT = 0.25
  )
}
}
\references{
Maurer, W., and Bretz, F. (2013). Multiple testing in group sequential
trials using graphical approaches. \emph{Statistics in Biopharmaceutical
Research}, 5(4), 311-320.

Wassmer, G., and Pahlke, F. rpact: Confirmatory Adaptive Clinical Trial
Design and Analysis. R package.
\url{https://CRAN.R-project.org/package=rpact}
}
\seealso{
\code{\link[=graph_test_shortcut_gsd]{graph_test_shortcut_gsd()}} for the same procedure with the built-in
spending functions, \code{\link[=graph_test_shortcut_gsDesign]{graph_test_shortcut_gsDesign()}} for the same
procedure with gsDesign, \code{\link[=graph_test_shortcut]{graph_test_shortcut()}} for the fixed-sample
procedure, \code{\link[rpact:getDesignGroupSequential]{rpact::getDesignGroupSequential()}} for the rpact function
used.
}
