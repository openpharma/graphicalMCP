% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsd_engine.R
\name{gsd_engine_gsDesign}
\alias{gsd_engine_gsDesign}
\title{Group sequential engine backed by gsDesign}
\usage{
gsd_engine_gsDesign(alpha, info_frac, sfu, sfupar, usTime)
}
\arguments{
\item{alpha}{Overall significance level, used to build the per-hypothesis
design objects read by \code{sequentialPValue()} (the level actually tested is
supplied per call).}

\item{info_frac}{Information fraction matrix (m x K), \code{NA}-padded.}

\item{sfu}{A list of m upper spending functions in gsDesign's convention,
each either a spending function such as \code{gsDesign::sfLDOF} or one of the
strings \code{"OF"}, \code{"Pocock"}, \code{"WT"}.}

\item{sfupar}{A list of m spending function parameters (\code{NULL} to use
gsDesign's default).}

\item{usTime}{\code{NULL}, or an m x K matrix of spending times with \code{NA} in the
same positions as \code{info_frac}.}
}
\value{
A list with the functions \code{repeated_p} and \code{nominal_bounds}; see
\code{\link[=gsd_engine_mvtnorm]{gsd_engine_mvtnorm()}}.
}
\description{
\code{gsd_engine_gsDesign()} builds the engine used by
\code{\link[=graph_test_shortcut_gsDesign]{graph_test_shortcut_gsDesign()}}. It has the same two-function interface as
\code{\link[=gsd_engine_mvtnorm]{gsd_engine_mvtnorm()}} but obtains every group sequential quantity from the
gsDesign package:
\itemize{
\item One design per hypothesis encodes its analysis schedule,
\code{gsDesign::gsDesign(k, test.type = 1, sfu, sfupar, n.I = info_frac, maxn.IPlan = 1, usTime)}, with information given as fractions of the
planned maximum so that the correlation between analyses is the one
implied by \code{info_frac}, and \code{usTime} controlling how alpha is spent.
\item Repeated p-values come from \code{gsDesign::sequentialPValue()} after setting
the Z-statistics of the earlier analyses to -20 (gsDesign's own value for
"no lower bound"), so that only the current analysis can trigger a
rejection. For the classical boundary families that gsDesign represents
as boundary shapes rather than spending functions (\code{sfu} given as \code{"OF"},
\code{"Pocock"}, or \code{"WT"}), \code{sequentialPValue()} is not available, and the
repeated p-value is instead found by solving \eqn{b_k(\alpha) = Z_k} on
the design's upper boundary.
\item Nominal boundaries are \code{pnorm(gsDesign(...)$upper$bound, lower.tail = FALSE)} with the design rebuilt at the allocated significance level.
\item A hypothesis with a single analysis uses the univariate results directly,
since \code{gsDesign()} requires at least two analyses.
}

The spending time is always passed to gsDesign explicitly (\code{usTime} if
supplied, otherwise \code{pmin(info_frac, 1)}), because \code{sequentialPValue()}'s
default rescales it by the last observed information and would change the
interim spending when a trial over-runs its planned information. \code{beta} is
set to \code{(1 - alpha) / 2} in every design call: it does not affect the upper
boundary for \code{test.type = 1} and only has to satisfy \code{alpha + beta < 1}.
}
\keyword{internal}
