% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsd_engine.R
\name{gsd_engine_rpact}
\alias{gsd_engine_rpact}
\title{Group sequential engine backed by rpact}
\usage{
gsd_engine_rpact(
  alpha,
  info_frac,
  typeOfDesign,
  gammaA,
  deltaWT,
  userAlphaSpending,
  spending_time
)
}
\arguments{
\item{alpha}{Overall significance level, used to build the per-hypothesis
design objects read by \code{sequentialPValue()} (the level actually tested is
supplied per call).}

\item{info_frac}{Information fraction matrix (m x K), \code{NA}-padded.}

\item{typeOfDesign}{A list of m design types in rpact's convention, e.g.
\code{"asOF"}, \code{"asP"}, \code{"asHSD"}, \code{"asKD"}, \code{"asUser"}, \code{"OF"}, \code{"P"},
\code{"WT"}.}

\item{gammaA, deltaWT, userAlphaSpending}{Lists of m design parameters
(\code{NULL} where not applicable): \code{gammaA} for \code{"asHSD"} and \code{"asKD"},
\code{deltaWT} for \code{"WT"}, and the cumulative alpha spent at each analysis
for \code{"asUser"}, scaled proportionally to the allocated level.}

\item{spending_time}{\code{NULL}, or an m x K matrix of spending times with \code{NA}
in the same positions as \code{info_frac}.}
}
\value{
A list with the functions \code{repeated_p} and \code{nominal_bounds}; see
\code{\link[=gsd_engine_mvtnorm]{gsd_engine_mvtnorm()}}.
}
\description{
\code{gsd_engine_rpact()} builds the engine used by \code{\link[=graph_test_shortcut_rpact]{graph_test_shortcut_rpact()}}.
It has the same two-function interface as \code{\link[=gsd_engine_mvtnorm]{gsd_engine_mvtnorm()}} and obtains
every group sequential quantity from \code{\link[rpact:getDesignGroupSequential]{rpact::getDesignGroupSequential()}}:
\itemize{
\item One design per hypothesis at the allocated level encodes its analysis
schedule, \code{getDesignGroupSequential(kMax = K_j, informationRates, alpha, sided = 1, typeOfDesign, ...)}. Nominal boundaries are its \code{stageLevels}.
\item Repeated p-values are found by solving \eqn{c_k(\alpha) = Z_k} on the
design's \code{criticalValues} over the level. This is the algorithm that
rpact's own \code{getRepeatedPValues()} uses internally (a bisection over the
level with the design rebuilt at each step), applied without its ceiling
of 0.5 and to every design type.
\item rpact evaluates the spending function at the design's information rates
and does not accept rates above 1. A spending time that differs from the
information fractions, or information fractions above 1 (over-running
information), is therefore expressed through an \code{"asUser"} design: the
cumulative spending is taken from a design at the spending time
(\code{alphaSpent}) and passed as \code{userAlphaSpending} to a design at the
information fractions, which are divided by their maximum when they
exceed 1 (the correlation between analyses depends only on their
ratios). This does not apply to the classical boundary families, which
are defined by their boundary shape.
\item A hypothesis with a single analysis below full information uses the
first stage of a two-stage design ending at 1; at full information it is
a fixed-sample test.
}

rpact warns when a level lies outside its validated range \verb{[1e-6, 0.5)},
which the engine muffles, and cannot compute designs at levels above
roughly 0.65 ("critical values cannot be calculated"). The engine searches
up to the highest level rpact can compute and reports a repeated p-value
of 1 when the boundary is not crossed there.
}
\keyword{internal}
