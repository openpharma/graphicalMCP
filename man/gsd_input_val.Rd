% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph_test_shortcut_gsd.R
\name{gsd_input_val}
\alias{gsd_input_val}
\title{Validate inputs for group sequential graphical MCP}
\usage{
gsd_input_val(
  graph,
  p,
  alpha,
  info_frac,
  spending_fn,
  look_back,
  verbose,
  test_values
)
}
\arguments{
\item{graph}{An initial graph as returned by \code{\link[=graph_create]{graph_create()}}.}

\item{p}{A numeric matrix of p-values with \eqn{m} rows (hypotheses) and
\eqn{K} columns (analyses).}

\item{alpha}{A numeric scalar of the overall significance level, which should
be between 0 & 1. The default is 0.025 for one-sided hypothesis testing
problems; another common choice is 0.05 for two-sided hypothesis testing
problems.}

\item{info_frac}{Information fractions at each analysis. Can be:
\itemize{
\item A numeric vector of length \eqn{K} — same fractions for all hypotheses.
\item A numeric matrix with \eqn{m} rows (hypotheses) and \eqn{K} columns
(analyses) — different fractions per hypothesis.
}

All hypotheses must have the same number of analyses (\eqn{K}). Values
must be in (0, 1] and monotonically non-decreasing per hypothesis. The
last value does not need to be 1, allowing the procedure to be applied
up to an interim analysis.}

\item{spending_fn}{Spending function(s) for computing group sequential
boundaries. Can be:
\itemize{
\item A single function — applied to all hypotheses.
\item A list of \eqn{m} functions — one per hypothesis.
}

Each function must accept two arguments: \code{alpha} (significance level)
and \code{info_frac} (information fraction), and return the cumulative alpha
spent. Built-in options include \code{\link[=spending_of]{spending_of()}}, \code{\link[=spending_pocock]{spending_pocock()}},
\code{\link[=spending_hsd]{spending_hsd()}}, and \code{\link[=spending_linear]{spending_linear()}}.}

\item{look_back}{A logical scalar controlling the testing strategy.
If \code{FALSE} (default), rejection decisions at each analysis are based on
repeated p-values at that analysis only. If \code{TRUE}, rejection decisions
are based on sequential p-values which consider all analyses up to the
current one. The \code{look_back = TRUE} option can lead to additional
rejections because a hypothesis may have crossed its boundary at an
earlier analysis but only becomes testable (via graph update) at a later
analysis. The default is \code{FALSE}.}

\item{verbose}{A logical scalar specifying whether the details of
intermediate update graphs should be included in results. When
\code{verbose = TRUE}, intermediate update graphs are provided after deleting
each hypothesis, which has been rejected. The default is \code{verbose = FALSE}.}

\item{test_values}{A logical scalar specifying whether to include the
per-analysis rejection details in results. When \code{test_values = TRUE}, the
rejection sequence, analysis at which each rejection occurred, and the
nominal p-value boundaries are reported. The default is
\code{test_values = FALSE}.}
}
\value{
Invisibly returns \code{graph}.
}
\description{
Validate inputs for group sequential graphical MCP
}
\keyword{internal}
